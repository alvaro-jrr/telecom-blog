---
import { SITE_DESCRIPTION, SITE_TITLE } from "@/consts";
import BaseLayout from "@/layouts/BaseLayout.astro";

import telecomTowerImage from "@/assets/telecom-tower.jpeg";
import PostsList from "@/components/PostsList.astro";
import { Image } from "astro:assets";
---

<BaseLayout
	title={SITE_TITLE}
	description={SITE_DESCRIPTION}
	class="space-y-20 py-20"
>
	<section>
		<div class="space-y-4 sm:text-center">
			<h1 class="text-4xl font-bold">Sistemas de Comunicación</h1>

			<p class="text-2xl text-muted-foreground">
				Conoce más sobre el campo de las comunicaciones electrónicas
			</p>
		</div>

		<Image
			src={telecomTowerImage}
			alt="Torre de Comunicación"
			class="mt-20 max-h-72 w-full rounded-md object-cover"
			loading="eager"
		/>
	</section>

	<section class="space-y-6">
		<h2 class="text-2xl font-bold">Publicaciones recientes</h2>

		<PostsList
			class="flex flex-col gap-4"
			emptyMessage="No hay publicaciones recientes"
		/>
	</section>
</BaseLayout>
